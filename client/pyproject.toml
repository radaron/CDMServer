[project]
name = "cdmclient"
version = "1.2.5"
description = "CDM - Centralized Download Manager Client"
authors = [{name = "Aron Radics", email = "radics.aron.jozsef@gmail.com"}]
requires-python = ">=3.9"
readme = "README.md"
license = {file = "LICENSE"}
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]
dependencies = [
  "requests",
  "transmission_rpc",
  "cryptography",
  "sqlalchemy>=2.0.0",
  "qbittorrent-api>=2025.7.0",
]

[project.urls]
Homepage = "https://github.com/radaron/CDMServer/tree/master/client"
Repository = "https://github.com/radaron/CDMServer"

[project.scripts]
cdm-client = "cdm_client.cdm_client:main"

[dependency-groups]
dev = [
    "dev-dependencies",
    "types-requests>=2.32.4.20250913",
]

[tool.uv.sources]
dev-dependencies = { path = "../common/dev_dependencies", editable = true }

[tool.ty.environment]
python-version = "3.9"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["cdm_client"]
