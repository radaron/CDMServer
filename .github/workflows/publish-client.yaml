name: Publish Client to PyPI

on:
  workflow_dispatch:
  push:
    branches:
      - master
    paths:
      - 'client/pyproject.toml'

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Install uv
        uses: astral-sh/setup-uv@v6

      - name: Install dependencies
        run: make reqs-ci

      - name: Check formatting
        working-directory: client
        run: make check-format-ci

      - name: Lint
        working-directory: client
        run: make lint

      - name: Get version
        id: get_version
        working-directory: client
        run: |
          VERSION=$(uv version --short)
          echo "VERSION=$VERSION" >> $GITHUB_OUTPUT

      - name: Build and publish
        working-directory: client
        run: |
          uv build --out-dir dist
          uv publish
        env:
          UV_PUBLISH_TOKEN: ${{ secrets.CLIENT_PYPI_TOKEN }}

      - name: Create and push git tag
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git config --global user.name "github-actions[bot]"
          TAG=cdmclient-${{ steps.get_version.outputs.VERSION }}
          git tag $TAG
          git push https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git $TAG
